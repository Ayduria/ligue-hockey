#include "JoueurAutonome.h"

JoueurAutonome::JoueurAutonome(string prenom, string nom, float taille, float poids, string ville, Parcours parcours) : Joueur(prenom, nom, taille, poids, ville, parcours) {}